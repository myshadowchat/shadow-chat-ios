{
  "name": "shadow-chat-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Native iOS wrapper (Capacitor) for Shadow Chat — loads https://myshadow.live",
  "scripts": {
    "ios:add": "cap add ios",
    "ios:sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
